{
  "name": "@aura-importer/web",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "tsc -p tsconfig.json && vite build && node ../scripts/generate-cache-manifest.mjs",
    "preview": "vite preview --host 127.0.0.1"
  },
  "dependencies": {
    "@aura-importer/converter": "0.1.0",
    "@sqlite.org/sqlite-wasm": "^3.51.2-build6",
    "fflate": "^0.8.2",
    "lucide-react": "^0.561.0",
    "react": "^19.2.1",
    "react-dom": "^19.2.1"
  }
}
